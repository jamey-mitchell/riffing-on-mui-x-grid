export enum OverflySelection {
  '',
  'direct',
  'intercept',
}

export enum AltitudeRestrictionSelection {
  '',
  'at/above',
  'at/below',
  'between',
}

export enum SpeedRestrictionSelection {
  '',
  'at speed',
  'speed',
}

export enum TimeRestrictionTypeSelection {
  '',
  'advisory',
  'fixed',
}
